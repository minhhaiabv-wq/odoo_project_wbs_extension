<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_project_issue_tree" model="ir.ui.view">
        <field name="name">project.issue.tree</field>
        <field name="model">project.issue</field>
        <field name="arch" type="xml">
            <list string="Issues" decoration-danger="priority == '3'" decoration-warning="priority == '2'" decoration-muted="state == 'closed'">
                <field name="priority" widget="priority"/>
                <field name="name"/>
                <field name="project_id"/>
                <field name="task_id"/>
                <field name="phase_id"/>
                <field name="assigned_to"/>
                <field name="state" widget="badge" decoration-info="state == 'open'" decoration-success="state == 'resolved'"/>
                <field name="date_reported"/>
            </list>
        </field>
    </record>

    <record id="view_project_issue_form" model="ir.ui.view">
        <field name="name">project.issue.form</field>
        <field name="model">project.issue</field>
        <field name="arch" type="xml">
            <form string="Issue">
                <header>
                    <field name="can_change_state" invisible="1"/>
                    <field name="is_leader_or_manager" invisible="1"/>
                    <button name="action_confirm" string="Confirm" type="object" invisible="state != 'draft' or not can_change_state" class="oe_highlight"/>
                    <button name="action_resolve" string="Resolve" type="object" invisible="state != 'open' or not can_change_state" class="oe_highlight"/>
                    <button name="action_close" string="Close" type="object" invisible="state != 'resolved' or not can_change_state"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,open,resolved,closed" options="{'clickable': '1'}" invisible="not is_leader_or_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,open,resolved,closed" invisible="is_leader_or_manager"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Issue Title"/>
                        <h1>
                            <div class="d-flex">
                                <field name="priority" widget="priority" class="me-2" nolabel="1"/>
                                <field class="text-break" name="name" options="{'line_breaks': False}" widget="text" placeholder="Issue Title..."/>
                            </div>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="project_id" domain="[('is_template', '=', False)]"/>
                            <field name="project_phase_ids" invisible="1"/>
                            <field name="task_id" domain="[('project_id', '=', project_id)]"/>
                            <field name="phase_id" domain="[('id', 'in', project_phase_ids)]"/>
                        </group>
                        <group>
                            <field name="project_member_ids" invisible="1"/>
                            <field name="reported_by" domain="[('id', 'in', project_member_ids)]"/>
                            <field name="assigned_to" domain="[('id', 'in', project_member_ids)]"/>
                            <field name="date_reported"/>
                            <field name="date_resolved" invisible="state != 'resolved' and state != 'closed'"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Content">
                            <field name="content" placeholder="Write Issue content here..."/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_project_issue_search" model="ir.ui.view">
        <field name="name">project.issue.search</field>
        <field name="model">project.issue</field>
        <field name="arch" type="xml">
            <search string="Search Issues">
                <field name="name"/>
                <field name="project_id"/>
                <field name="task_id"/>
                <field name="phase_id"/>
                <field name="assigned_to"/>
                <filter string="My Reported" name="my_reported" domain="[('reported_by', '=', uid)]"/>
                <filter string="My Issues" name="my_issues" domain="[('assigned_to', '=', uid)]"/>
                <separator/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Open" name="open" domain="[('state', '=', 'open')]"/>
                <filter string="Resolved" name="resolved" domain="[('state', '=', 'resolved')]"/>
                <filter string="Closed" name="closed" domain="[('state', '=', 'closed')]"/>
                <group>
                    <filter string="Project" name="group_by_project" context="{'group_by': 'project_id'}"/>
                    <filter string="Task" name="group_by_task" context="{'group_by': 'task_id'}"/>
                    <filter string="Phase" name="group_by_phase" context="{'group_by': 'phase_id'}"/>
                    <filter string="Status" name="group_by_status" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_project_issue" model="ir.actions.act_window">
        <field name="name">Issues</field>
        <field name="res_model">project.issue</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_project_issue_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Record a new issue
            </p>
        </field>
    </record>

    <menuitem id="menu_project_issue"
              name="Issues"
              parent="project.menu_main_pm"
              action="action_project_issue"
              sequence="6"/>
</odoo>
